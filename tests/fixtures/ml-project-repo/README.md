# ML Project Repo

Keywords: machine learning, benchmark dataset, seeded baseline method.

## Installation

Use Python 3.10.

## Usage

```bash
python scripts/train.py --config configs/default.yaml
```

## Reproducibility

The training script sets a deterministic seed and writes toy metrics.

## Data availability

See `data/README.md`.
