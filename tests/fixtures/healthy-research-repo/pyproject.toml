[project]
name = "healthy-research-repo"
version = "0.1.0"
description = "A healthy fixture repository."
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
