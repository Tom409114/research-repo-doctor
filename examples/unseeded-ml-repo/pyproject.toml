[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "unseeded-ml-demo"
version = "0.1.0"
description = "Tiny intentionally incomplete demo repo for rrdoctor."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["numpy>=1.26"]
