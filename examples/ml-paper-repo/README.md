# ML Paper Repo

Keywords: ML benchmark, reproducible baseline, toy dataset.

## Installation

Use Python 3.10 and install the project from `pyproject.toml`.

## Usage

Run `python scripts/train.py --config configs/default.yaml`.

## Reproducibility

The training script sets a fixed seed and writes deterministic toy results.

## Data availability

See `data/README.md`.

## Citation

See `CITATION.cff`.
